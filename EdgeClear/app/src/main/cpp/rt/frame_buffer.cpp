// frame_buffer.cpp - Implementation file
// DSPFrameBuffer is header-only (inline methods), but CMakeLists.txt expects .cpp
#include "frame_buffer.h"

// Empty translation unit - all implementation is in header
